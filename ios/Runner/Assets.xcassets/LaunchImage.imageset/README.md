# Generated Launch Screen Assets

The files in this image set are generated by `flutter_native_splash` from the
configuration in `pubspec.yaml` and the source images in `assets/branding/`.
Do not replace or edit the generated iOS assets manually.

After changing the source branding files or splash configuration, regenerate
the native launch screen from the repository root:

```bash
dart run flutter_native_splash:create
```
